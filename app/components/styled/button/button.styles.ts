import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  title: {
    fontSize: 24,
    letterSpacing: 0,
    textAlign: "center",
  },
  paragraph: {
    fontSize: 18,
    letterSpacing: 0,
    textAlign: "center",
  },
});
